.row
  .large-6.columns
    h1 Create an Account

    form
      = input valueBinding='email' placeholder='Email'
      = input valueBinding='password' placeholder='Password'
      = input valueBinding='passwordConfirmation' placeholder='Password Confirmation'

      button.small.radius.right{action 'submit'}
        |Sign Up
      button.small.radius.right{action 'cancel'}
        |Cancel

